{
  "id": "memory",
  "type": "atomic",
  "inputs": ["memory_stream", "window_size"],
  "outputs": ["active_memory", "pruned_flag"],
  "semantics": [
    "Đại diện cho bộ nhớ làm việc (working memory) tích lũy và quản lý dung lượng hội thoại.",
    "Cắt tỉa và duy trì các thông tin quan trọng nhất."
  ],
  "compositions": [
    "context + memory -> context-memory"
  ]
}
