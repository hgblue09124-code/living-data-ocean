{
  "id": "select",
  "type": "atomic",
  "inputs": ["candidates_list", "selection_criteria"],
  "outputs": ["selected_option", "index"],
  "semantics": [
    "Lựa chọn phương án tối ưu nhất từ tập ứng viên cho trước.",
    "Đánh giá và trích chọn phương án khớp tiêu chí nhất."
  ],
  "compositions": [
    "select + option -> select-candidate"
  ]
}
