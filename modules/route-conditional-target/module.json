{
  "identity": {
    "id": "route-conditional-target",
    "name": "Route Conditional Target",
    "version": "1.0.0",
    "category": "Core",
    "verb": "route"
  },
  "purpose": "Routes dynamic input payload to target handler key based on rule conditions.",
  "capability": "Evaluates list of route condition rules and selects first matching destination.",
  "input": {
    "payload": "dict",
    "routes": "list of dicts with 'key', 'operator', 'value', 'target'",
    "default_target": "string (optional, default 'default')"
  },
  "output": {
    "target": "string",
    "matched_rule": "dict or None"
  },
  "interface": "index.py -> route_conditional_target(payload: dict, routes: list, default_target: str = 'default') -> dict",
  "dependencies": [],
  "boundary": "In-memory control flow routing.",
  "composition": "Composes with check-condition-predicate and classify-text-label.",
  "failure_behavior": "Falls back to default_target if no rule matches.",
  "operational_readiness": "Production ready. Pure Python."
}
