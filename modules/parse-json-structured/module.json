{
  "identity": {
    "id": "parse-json-structured",
    "name": "Parse JSON Structured",
    "version": "1.0.0",
    "category": "Core",
    "verb": "parse"
  },
  "purpose": "Parses a JSON string or bytes into structured Python data.",
  "capability": "Parses JSON safely with error handling and optional default fallback.",
  "input": {
    "data": "string or bytes",
    "fallback": "any (optional, default None)"
  },
  "output": {
    "parsed": "dict | list | primitive",
    "success": "boolean",
    "error": "string or None"
  },
  "interface": "index.py -> parse_json_structured(data: str | bytes, fallback: Any = None) -> dict",
  "dependencies": [],
  "boundary": "JSON parsing only.",
  "composition": "Used after read-file-content or retrieve-key-value.",
  "failure_behavior": "Returns success=False with error message if invalid JSON (or fallback if provided).",
  "operational_readiness": "Production ready. Uses standard json library."
}
